import os; KB_FOLDER = r"D:\AGI_Universal_Project\Knowledge_Base"; files = [f for f in os.listdir(KB_FOLDER) if f.endswith(".md")]; missing = []; print(f"Scanning {len(files)} files..."); [missing.append(f) for f in files if "source:" not in open(os.path.join(KB_FOLDER, f), "r", encoding="utf-8").read().lower()]; print(f"Missing Source ({len(missing)}):", missing if missing else "None")